#include <chrono>
#include <iostream>
#include <thread>
#include <conio.h>
using namespace std;

int main()
{
    int SpawnpointSnake[2] = {5,5};
    int PositionSnake[2];
    char key = ' ';
    while (true)
    {
        key = _getch();
        if (key == 'w')
        {
            
        }
    }
    cout << "You entered: " << key << endl;
}